{
  "name": "shortboxerr-dev",
  "dockerComposeFile": ["../docker-compose.dev.yml"],
  "service": "dev",
  "workspaceFolder": "/workspaces/shortboxerr",
  "containerEnv": {
    "GH_TOKEN": "${localEnv:GH_TOKEN}"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      },
      "extensions": [
        "ms-dotnettools.csharp",
        "ms-azuretools.vscode-docker",
        "editorconfig.editorconfig"
      ]
    }
  },
  "postCreateCommand": "dotnet --info && (sudo chown -R vscode:vscode /home/vscode/.nuget /workspaces/shortboxerr 2>/dev/null || true) && ( [ -z \"${GH_TOKEN}\" ] || echo \"${GH_TOKEN}\" | gh auth login --with-token )"
}
